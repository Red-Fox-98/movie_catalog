import type { NextPage } from 'next';
import MainTemplate from '../widgets/template/MainTemplate/MainTemplate';

const Home: NextPage = () => {
  return (
    <MainTemplate>
      <div>content</div>
    </MainTemplate>
  );
};

export default Home;
